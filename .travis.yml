#
# K2HDKC
#
# Copyright 2017 Yahoo! JAPAN corporation.
#
# K2HDKC is k2hash based distributed KVS cluster.
# K2HDKC uses K2HASH, CHMPX, FULLOCK libraries. K2HDKC supports
# distributed KVS cluster server program and client libraries.
#
# For the full copyright and license information, please view
# the license file that was distributed with this source code.
#
# AUTHOR:   Takeshi Nakatani
# CREATE:   Wed 26 Dec 2018
# REVISION:
#

language: cpp

sudo: required

dist: trusty

env:
  global:
    - PCUSER=antpickax
    - PCREPO=stable
  matrix:
    - OS=ubuntu 
      TAG=18.04 
      NODE_MAJOR_VERSION=8
      PUBLISHER=false
      INSTPKG="k2hdkc-dev"
    - OS=ubuntu 
      TAG=18.04 
      NODE_MAJOR_VERSION=10
      PUBLISHER=true
      INSTPKG="k2hdkc-dev"
    - OS=ubuntu 
      TAG=18.04 
      NODE_MAJOR_VERSION=12
      PUBLISHER=false
      INSTPKG="k2hdkc-dev"

cache: apt

services:
  - docker

before_install:
  - echo 'DOCKER_OPTS="-H tcp://127.0.0.1:2375 -H unix:///var/run/docker.sock -s devicemapper"' | sudo tee /etc/default/docker > /dev/null
  - sudo service docker restart
  - sleep 5
  - sudo docker image pull ${OS}:${TAG}

script:
  - sudo docker container run 
    --rm=true 
    -e TRAVIS_PULL_REQUEST=${TRAVIS_PULL_REQUEST} 
    -e TRAVIS_TAG=${TRAVIS_TAG} 
    -e FORCE_PUBLISH_PKG=${FORCE_PUBLISH_PKG} 
    -e NPM_TOKEN=${NPM_TOKEN} 
    -e NODE_MAJOR_VERSION=${NODE_MAJOR_VERSION} 
    -e USE_PC_REPO=${USE_PC_REPO} 
    -e PUBLISHER=${PUBLISHER} 
    -v `pwd`:/k2hdkc_nodejs:rw
    ${OS}:${TAG} 
    /bin/sh -c "/k2hdkc_nodejs/buildutils/travis_builder.sh -pcuser ${PCUSER} -pcrepo ${PCREPO} ${INSTPKG}"

#
# VIM modelines
#
# vim:set ts=4 fenc=utf-8:
#
